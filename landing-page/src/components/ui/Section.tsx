export const Section = ({ children }: { children: React.ReactNode }) => {
    return (
      <div className="tw-container motion-preset-confetti w-full">
        {children}
      </div>
    );
  };
  